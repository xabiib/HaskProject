1) mkdir HaskProject
2) cd HaskProject
3) sqlite3 project.db < data.sql
4) cabal init --interactive
5) cabal run
6) add HaskProject.cabal file by adding ", sqlite-simple" into build-depends
7)

How-to: Use ANSI colors in the terminal
https://ss64.com/nt/syntax-ansi.html 

https://hackage.haskell.org/package/sqlite-simple-0.4.18.2/docs/Database-SQLite-Simple-FromRow.html
.tables

select * from student , course , registration where student.id = registration.student_id and course.id = registration.course_id ;

select student.id , student.username , course.name from student , course , registration where student.id = registration.student_id and course.id = registration.course_id ;

SELECT sql FROM sqlite_master WHERE tbl_name = 'student' AND type = 'table'